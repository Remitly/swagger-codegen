{{>partial_header}}
package main

import (
    "{{goModule}}/{{packageName}}/runtime"
)


// Place service specific code in service/api_{name}.go and service/customconfig.go
func main() {
    boot := runtime.Bootstrap{}
    boot.LaunchServer()
}
